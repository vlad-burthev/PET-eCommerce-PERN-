import type { FC } from "react";

interface FooterProps {}

import styles from "./Footer.module.scss";

const Footer: FC<FooterProps> = () => {
  return <footer className={styles.footer}>dasdas</footer>;
};

export default Footer;
